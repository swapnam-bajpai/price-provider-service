package api;

/**
 * Flexible data structure that can hold price data as per the use case<br>
 * For example, the monetary value and the currency may be part of information stored
 */
public record PriceData(long value) {
}